<h1>Welcome to the top secret prefrences page!</h1>

<p>Please select your preferences below:</p>

<%= form_for [:admin, @preference], :url => admin_preference_path(@preference) do |f| %>
  Artist Sort Order: <%= f.select :artist_sort_order, ["ASC", "DESC"] %><br>
  Song Sort Order: <%= f.select :song_sort_order, ["ASC", "DESC"] %><br>
  Allow Creating Artists? <%= f.select :allow_create_artists, [true, false] %><br>
  Allow Creating Songs? <%= f.select :allow_create_songs, [true, false] %><br><br>

  <%= f.submit "Update Preferences" %>
<% end %>
