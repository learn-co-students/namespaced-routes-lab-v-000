<h1>Flatiron Tunes Library</h1>
<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
<br>
<%= link_to "Add New Artist", new_artist_path %>
